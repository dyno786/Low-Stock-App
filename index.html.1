<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>Low Stock Checker</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600&family=DM+Mono&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jsbarcode/3.11.6/JsBarcode.all.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js"></script>
<style>
:root{--bg:#f3f1ed;--surface:#fff;--s2:#f8f7f4;--border:#e5e1d8;--text:#18160f;--muted:#7a7465;--red:#b91c1c;--red-bg:#fef2f2;--amber:#b45309;--amber-bg:#fffbeb;--green:#15803d;--green-bg:#f0fdf4;--purple:#6d28d9;--purple-bg:#ede9fe;--wa:#25d366;--r:14px;--rs:9px;--font:'DM Sans',sans-serif;--mono:'DM Mono',monospace}
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
body{font-family:var(--font);background:var(--bg);color:var(--text);max-width:480px;margin:0 auto;min-height:100vh;padding-bottom:90px}
/* TOP */
.topbar{background:var(--text);color:#fff;padding:14px 16px 12px;position:sticky;top:0;z-index:100}
.tb-row{display:flex;align-items:center;justify-content:space-between;gap:8px}
.store-name{font-size:16px;font-weight:600;letter-spacing:-.3px}
.store-sub{font-size:11px;color:#999;margin-top:2px;font-family:var(--mono)}
.tb-btns{display:flex;gap:6px;flex-shrink:0}
.icon-btn{background:rgba(255,255,255,.12);border:none;color:#fff;border-radius:8px;padding:7px 10px;font-size:11px;font-weight:600;cursor:pointer;font-family:var(--font);display:flex;align-items:center;gap:4px;white-space:nowrap}
.icon-btn.spinning svg{animation:spin .8s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.summary-pills{display:flex;gap:6px;margin-top:10px;flex-wrap:wrap}
.pill{font-size:11px;font-weight:600;padding:4px 10px;border-radius:20px;border:1px solid rgba(255,255,255,.12)}
.pill-red{background:rgba(185,28,28,.3);color:#fca5a5}
.pill-done{background:rgba(21,128,61,.3);color:#86efac}
.pill-time{background:rgba(255,255,255,.08);color:#aaa}
/* STORE TABS */
.store-bar{display:flex;background:var(--surface);border-bottom:1px solid var(--border);overflow-x:auto;scrollbar-width:none;position:sticky;top:78px;z-index:98}
.store-bar::-webkit-scrollbar{display:none}
.store-tab{padding:11px 16px;font-size:13px;font-weight:500;color:var(--muted);border-bottom:2.5px solid transparent;cursor:pointer;white-space:nowrap;flex-shrink:0;transition:all .15s}
.store-tab.active{color:var(--text);border-bottom-color:var(--text)}
.add-tab{padding:11px 14px;font-size:13px;color:var(--muted);cursor:pointer;flex-shrink:0;display:flex;align-items:center;gap:5px;border-left:1px solid var(--border);white-space:nowrap}
.add-tab:active{color:var(--text)}
/* VIEW TABS */
.view-tabs{display:flex;background:var(--surface);border-bottom:1px solid var(--border);position:sticky;top:122px;z-index:97}
.tab{flex:1;padding:11px 4px;text-align:center;font-size:12px;font-weight:500;color:var(--muted);border-bottom:2.5px solid transparent;cursor:pointer;transition:all .15s}
.tab.active{color:var(--text);border-bottom-color:var(--text)}
/* TOOLBAR */
.toolbar{padding:10px 12px;background:var(--surface);border-bottom:1px solid var(--border);display:flex;gap:8px}
.sw{flex:1;position:relative}
.sw input{width:100%;background:var(--bg);border:1px solid var(--border);border-radius:var(--rs);padding:9px 12px 9px 32px;font-size:13px;font-family:var(--font);color:var(--text);outline:none}
.sw input::placeholder{color:var(--muted)}
.si{position:absolute;left:9px;top:50%;transform:translateY(-50%);color:var(--muted);pointer-events:none}
.scan-btn{background:var(--text);color:#fff;border:none;border-radius:var(--rs);padding:9px 13px;font-size:12px;font-weight:600;cursor:pointer;display:flex;align-items:center;gap:5px;white-space:nowrap}
/* WA BAR */
.wa-bar{background:var(--s2);border-bottom:1px solid var(--border);padding:9px 12px;display:flex;align-items:center;gap:8px}
.wa-bar input{flex:1;background:var(--bg);border:1px solid var(--border);border-radius:var(--rs);padding:7px 10px;font-size:12px;font-family:var(--mono);color:var(--text);outline:none}
.wa-bar input::placeholder{color:var(--muted);font-family:var(--font);font-size:11px}
.wa-bar button{background:var(--text);color:#fff;border:none;border-radius:var(--rs);padding:7px 12px;font-size:11px;font-weight:600;cursor:pointer}
.wa-lbl{font-size:11px;color:var(--muted);white-space:nowrap}
/* LIST */
.sec-head{padding:12px 12px 5px;font-size:10px;font-weight:600;letter-spacing:.9px;text-transform:uppercase;color:var(--muted);background:var(--bg);display:flex;justify-content:space-between;align-items:center}
.group-row{display:flex;align-items:center;background:var(--surface);padding:13px 12px;border-bottom:1px solid var(--border);gap:10px;cursor:pointer;transition:background .1s}
.group-row:active{background:var(--s2)}
.g-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0}
.g-info{flex:1;min-width:0}
.g-name{font-size:14px;font-weight:500;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.g-sub{font-size:11px;color:var(--muted);margin-top:1px}
.g-right{display:flex;align-items:center;gap:8px;flex-shrink:0}
.g-badge{font-size:11px;font-weight:600;padding:3px 9px;border-radius:20px}
.b-red{background:var(--red-bg);color:var(--red)}
.b-amber{background:var(--amber-bg);color:var(--amber)}
.chevron{color:var(--muted);font-size:17px}
/* PRODUCT CARDS */
.product-card{display:flex;background:var(--surface);border-bottom:1px solid var(--border);cursor:pointer;transition:background .1s;position:relative;min-height:80px}
.product-card:active{background:var(--s2)}
.p-img-wrap{width:80px;min-height:80px;flex-shrink:0;overflow:hidden;background:var(--bg);display:flex;align-items:center;justify-content:center;border-right:1px solid var(--border)}
.p-img-wrap img{width:80px;height:80px;object-fit:cover;display:block}
.p-body{flex:1;padding:11px 8px 11px 13px;min-width:0;display:flex;flex-direction:column;justify-content:center}
.p-brand{font-size:10px;font-weight:600;color:var(--purple);text-transform:uppercase;letter-spacing:.5px;margin-bottom:2px}
.p-name{font-size:13px;font-weight:500;line-height:1.35;margin-bottom:3px}
.p-variant{font-size:11px;color:var(--muted)}
.p-right{display:flex;flex-direction:column;align-items:flex-end;justify-content:space-between;padding:11px 12px 11px 4px;flex-shrink:0;gap:8px}
.p-qty{font-size:16px;font-weight:700;font-family:var(--mono)}
.qty-neg{color:var(--red)}.qty-pos{color:var(--green)}
.tick-circle{width:32px;height:32px;border-radius:50%;border:2px solid var(--border);display:flex;align-items:center;justify-content:center;transition:all .2s;flex-shrink:0}
.tick-circle.done{background:var(--green);border-color:var(--green)}
.tick-circle svg{opacity:0;transition:opacity .15s}
.tick-circle.done svg{opacity:1}
.product-card.ticked .p-name{text-decoration:line-through;color:var(--muted)}
.done-stripe{position:absolute;inset:0;background:rgba(21,128,61,.04);pointer-events:none;display:none}
.product-card.ticked .done-stripe{display:block}
/* BACK BAR */
.back-bar{display:flex;align-items:center;gap:8px;padding:11px 12px;background:var(--surface);border-bottom:1px solid var(--border);cursor:pointer;position:sticky;top:160px;z-index:50}
.back-bar:active{background:var(--s2)}
/* STATES */
.loading{padding:56px 20px;text-align:center;color:var(--muted)}
.spinner{width:28px;height:28px;border:3px solid var(--border);border-top-color:var(--text);border-radius:50%;animation:spin .8s linear infinite;margin:0 auto 14px}
.error-card{margin:14px 12px;background:var(--surface);border:1px solid var(--border);border-left:3px solid var(--red);border-radius:var(--rs);padding:14px 16px}
.error-card p{font-size:13px;color:var(--red);line-height:1.5;margin-bottom:6px}
.error-card code{font-size:11px;color:var(--muted);font-family:var(--mono);word-break:break-all}
.empty-state{padding:56px 20px;text-align:center;color:var(--muted);font-size:14px;line-height:1.7}
/* MODAL */
.modal-bg{position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:400;display:flex;align-items:flex-end;max-width:480px;margin:0 auto}
.modal-bg.hidden{display:none}
.modal{background:var(--surface);border-radius:20px 20px 0 0;width:100%;max-height:90vh;overflow-y:auto;padding-bottom:40px}
.modal-handle{width:36px;height:4px;background:var(--border);border-radius:2px;margin:12px auto 0}
.modal-head{padding:16px 18px 0}
.modal-head h2{font-size:17px;font-weight:600;margin-bottom:4px}
.modal-head p{font-size:13px;color:var(--muted);line-height:1.55}
.modal-body{padding:14px 18px 0}
.field-label{font-size:11px;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:.5px;margin-bottom:5px;display:block}
.field-input{width:100%;background:var(--bg);border:1px solid var(--border);border-radius:var(--rs);padding:11px 13px;font-size:13px;font-family:var(--font);color:var(--text);outline:none;margin-bottom:4px}
.field-input:focus{border-color:var(--purple)}
.field-hint{font-size:11px;color:var(--muted);line-height:1.55;margin-bottom:14px}
.steps-box{background:var(--s2);border:1px solid var(--border);border-radius:var(--rs);padding:13px 14px;margin-bottom:16px}
.steps-box p{font-size:12px;font-weight:600;color:var(--text);margin-bottom:8px}
.step-item{display:flex;gap:10px;margin-bottom:7px;align-items:flex-start}
.step-num{background:var(--text);color:#fff;font-size:10px;font-weight:700;border-radius:50%;width:18px;height:18px;display:flex;align-items:center;justify-content:center;flex-shrink:0;margin-top:1px}
.step-item p{font-size:12px;color:var(--muted);line-height:1.5}
.step-item strong{color:var(--text)}
.modal-actions{padding:14px 18px 0;display:flex;gap:8px}
.btn-primary{flex:1;background:var(--text);color:#fff;border:none;border-radius:var(--rs);padding:14px;font-size:14px;font-weight:600;cursor:pointer;font-family:var(--font)}
.btn-secondary{background:var(--s2);color:var(--text);border:1px solid var(--border);border-radius:var(--rs);padding:14px 16px;font-size:14px;font-weight:500;cursor:pointer;font-family:var(--font)}
.delete-btn{display:flex;align-items:center;gap:6px;background:none;border:none;color:var(--red);font-size:12px;font-weight:500;cursor:pointer;font-family:var(--font);padding:10px 0 0}
/* DETAIL PANEL */
.panel-ov{position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:200;display:none;max-width:480px;margin:0 auto}
.panel-ov.open{display:block}
.panel{position:absolute;bottom:0;left:0;right:0;background:var(--surface);border-radius:20px 20px 0 0;max-height:92vh;overflow-y:auto;padding-bottom:36px}
.p-handle{width:36px;height:4px;background:var(--border);border-radius:2px;margin:12px auto 0}
.p-hero{width:100%;height:180px;background:var(--bg);display:flex;align-items:center;justify-content:center;overflow:hidden;border-bottom:1px solid var(--border)}
.p-hero img{max-height:170px;max-width:100%;object-fit:contain}
.p-det{padding:16px 16px 0}
.pd-brand{font-size:11px;font-weight:600;color:var(--purple);text-transform:uppercase;letter-spacing:.5px}
.pd-title{font-size:17px;font-weight:600;line-height:1.3;margin:4px 0 4px}
.pd-variant{font-size:13px;color:var(--muted)}
.pd-meta{display:flex;gap:7px;margin:8px 0 4px;flex-wrap:wrap}
.pd-chip{font-size:11px;padding:3px 10px;border-radius:20px;font-weight:500}
.chip-cat{background:var(--purple-bg);color:var(--purple)}
.chip-stock{background:var(--red-bg);color:var(--red);font-family:var(--mono)}
.pd-sku{font-size:11px;color:var(--muted);font-family:var(--mono);margin-bottom:12px}
.bc-wrap{background:#fff;border:1px solid var(--border);border-radius:var(--rs);padding:10px;text-align:center;margin-bottom:12px}
.bc-wrap svg{max-width:100%;height:auto}
.pd-div{height:1px;background:var(--border);margin:12px 0}
.restock-head{font-size:13px;font-weight:600;margin-bottom:10px}
.stepper-row{display:flex;align-items:center;gap:10px;margin-bottom:14px}
.step-lbl{font-size:13px;color:var(--muted);flex:1}
.step-btn{width:38px;height:38px;border-radius:9px;border:1px solid var(--border);background:var(--s2);font-size:22px;display:flex;align-items:center;justify-content:center;cursor:pointer;user-select:none;color:var(--text);line-height:1}
.step-btn:active{background:var(--border)}
.step-val{font-size:24px;font-weight:600;font-family:var(--mono);min-width:40px;text-align:center}
.p-actions{display:flex;gap:8px}
.save-btn{flex:1;background:var(--text);color:#fff;border:none;border-radius:var(--rs);padding:14px;font-size:14px;font-weight:600;cursor:pointer;font-family:var(--font)}
.done-btn{background:var(--green-bg);color:var(--green);border:1.5px solid var(--green);border-radius:var(--rs);padding:14px 14px;font-size:13px;font-weight:600;cursor:pointer;font-family:var(--font);display:flex;align-items:center;gap:5px;white-space:nowrap}
.done-btn.active{background:var(--green);color:#fff;border-color:var(--green)}
.saved-msg{font-size:12px;color:var(--green);text-align:center;margin-top:10px;font-weight:500;display:none}
/* WA FOOTER */
.wa-footer{position:fixed;bottom:0;left:0;right:0;max-width:480px;margin:0 auto;padding:10px 12px;background:var(--surface);border-top:1px solid var(--border);z-index:150}
.wa-btn{width:100%;background:var(--wa);color:#fff;border:none;border-radius:var(--rs);padding:14px;font-size:14px;font-weight:600;cursor:pointer;font-family:var(--font);display:flex;align-items:center;justify-content:center;gap:8px}
.wa-btn svg{width:20px;height:20px;flex-shrink:0}
.wa-cnt{background:#fff;color:var(--wa);border-radius:20px;font-size:11px;font-weight:700;padding:2px 8px}
/* SCANNER */
.scan-ov{position:fixed;inset:0;background:#000;z-index:500;display:none;flex-direction:column;max-width:480px;margin:0 auto}
.scan-ov.open{display:flex}
.scan-hdr{padding:14px 16px;display:flex;align-items:center;gap:10px}
.scan-close{background:rgba(255,255,255,.15);border:none;color:#fff;border-radius:50%;width:36px;height:36px;font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center}
.scan-title{color:#fff;font-size:15px;font-weight:600}
.scan-vw{flex:1;position:relative;display:flex;align-items:center;justify-content:center;overflow:hidden}
#scan-vid{width:100%;height:100%;object-fit:cover}
.scan-frame{position:absolute;width:240px;height:110px;border:2px solid var(--wa);border-radius:8px;box-shadow:0 0 0 9999px rgba(0,0,0,.5)}
.scan-hint{padding:14px;text-align:center;color:#999;font-size:12px}
.scan-manual{padding:0 14px 24px;display:flex;gap:8px}
.scan-manual input{flex:1;background:#1a1a1a;border:1px solid #333;border-radius:var(--rs);padding:10px 12px;font-size:14px;color:#fff;font-family:var(--font);outline:none}
.scan-manual button{background:var(--wa);color:#fff;border:none;border-radius:var(--rs);padding:10px 14px;font-size:14px;font-weight:600;cursor:pointer}
</style>
</head>
<body>

<div class="topbar">
  <div class="tb-row">
    <div>
      <div class="store-name" id="top-name">Low Stock</div>
      <div class="store-sub" id="top-date"></div>
    </div>
    <div class="tb-btns">
      <button class="icon-btn" id="refresh-btn" onclick="refreshCurrent()">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
        Refresh
      </button>
      <button class="icon-btn" onclick="openSettings()">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
        Stores
      </button>
    </div>
  </div>
  <div class="summary-pills">
    <div class="pill pill-red" id="pill-remain">—</div>
    <div class="pill pill-done" id="pill-done">0 checked ✓</div>
    <div class="pill pill-time" id="pill-time"></div>
  </div>
</div>

<div class="store-bar" id="store-bar"></div>

<div class="view-tabs">
  <div class="tab active" onclick="switchView('brand')">By brand</div>
  <div class="tab" onclick="switchView('category')">By category</div>
  <div class="tab" onclick="switchView('all')">All items</div>
</div>

<div class="toolbar">
  <div class="sw">
    <svg class="si" width="15" height="15" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2.2"><circle cx="8.5" cy="8.5" r="5.5"/><line x1="13" y1="13" x2="18" y2="18"/></svg>
    <input type="search" id="search-input" placeholder="Search products, brand, SKU…" oninput="renderList()">
  </div>
  <button class="scan-btn" onclick="openScanner()">
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="2" y="2" width="6" height="6" rx="1"/><rect x="16" y="2" width="6" height="6" rx="1"/><rect x="2" y="16" width="6" height="6" rx="1"/><line x1="22" y1="14" x2="14" y2="14"/><line x1="14" y1="14" x2="14" y2="22"/><line x1="22" y1="22" x2="18" y2="22"/><line x1="22" y1="18" x2="22" y2="22"/></svg>
    Scan
  </button>
</div>

<div class="wa-bar">
  <span class="wa-lbl">Manager WhatsApp:</span>
  <input type="tel" id="wa-number" placeholder="447700900000">
  <button onclick="saveWA()">Save</button>
</div>

<div id="main-content"></div>

<div class="wa-footer">
  <button class="wa-btn" onclick="sendWhatsApp()">
    <svg viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
    Send update to manager
    <span class="wa-cnt" id="wa-cnt">0</span>
  </button>
</div>

<!-- ADD / EDIT STORE MODAL -->
<div class="modal-bg hidden" id="store-modal" onclick="handleModalBg(event)">
  <div class="modal">
    <div class="modal-handle"></div>
    <div class="modal-head">
      <h2 id="modal-title">Add a store</h2>
      <p id="modal-desc">Connect a branch by pasting its Google Sheet published CSV link.</p>
    </div>
    <div class="modal-body">
      <div class="steps-box">
        <p>How to get your Google Sheet CSV link:</p>
        <div class="step-item"><div class="step-num">1</div><p>Open your Google Sheet → <strong>File → Share → Publish to web</strong></p></div>
        <div class="step-item"><div class="step-num">2</div><p>Select the <strong>tab for this store</strong> from the first dropdown</p></div>
        <div class="step-item"><div class="step-num">3</div><p>Change second dropdown to <strong>CSV</strong> then click <strong>Publish</strong></p></div>
        <div class="step-item"><div class="step-num">4</div><p>Copy the link it gives you and paste below</p></div>
      </div>
      <label class="field-label">Store name</label>
      <input class="field-input" type="text" id="modal-name" placeholder="e.g. Roundhay Road">
      <div class="field-hint">This is the label staff see in the tab bar.</div>
      <label class="field-label">Google Sheet CSV URL</label>
      <input class="field-input" type="url" id="modal-url" placeholder="https://docs.google.com/spreadsheets/d/…/pub?output=csv">
      <div class="field-hint">Must be a <strong>published</strong> CSV link. The URL should contain <code style="font-family:var(--mono)">/pub?</code> — not a regular share link.</div>
      <div id="modal-del-wrap" style="display:none">
        <button class="delete-btn" onclick="deleteStore()">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14H6L5 6"/><path d="M10 11v6M14 11v6"/><path d="M9 6V4h6v2"/></svg>
          Remove this store
        </button>
      </div>
    </div>
    <div class="modal-actions">
      <button class="btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn-primary" id="modal-save-btn" onclick="saveStore()">Connect store</button>
    </div>
  </div>
</div>

<!-- PRODUCT DETAIL PANEL -->
<div class="panel-ov" id="panel-ov" onclick="handlePanelBg(event)">
  <div class="panel">
    <div class="p-handle"></div>
    <div class="p-hero" id="pd-hero"></div>
    <div class="p-det">
      <div class="pd-brand" id="pd-brand"></div>
      <div class="pd-title" id="pd-title"></div>
      <div class="pd-variant" id="pd-variant"></div>
      <div class="pd-meta">
        <div class="pd-chip chip-cat" id="pd-cat"></div>
        <div class="pd-chip chip-stock" id="pd-stock"></div>
      </div>
      <div class="pd-sku" id="pd-sku"></div>
      <div id="bc-wrap" class="bc-wrap" style="display:none"><svg id="bc-svg"></svg></div>
      <div class="pd-div"></div>
      <div class="restock-head">How many units being put back on shelf?</div>
      <div class="stepper-row">
        <div class="step-lbl">Units restocked:</div>
        <div class="step-btn" onclick="step(-1)">−</div>
        <div class="step-val" id="step-val">0</div>
        <div class="step-btn" onclick="step(1)">+</div>
      </div>
      <div class="p-actions">
        <button class="save-btn" onclick="saveCount()">Save &amp; mark done</button>
        <button class="done-btn" id="done-btn" onclick="toggleTick()">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          Done
        </button>
      </div>
      <div class="saved-msg" id="saved-msg">✓ Saved — included in today's WhatsApp report</div>
    </div>
  </div>
</div>

<!-- SCANNER -->
<div class="scan-ov" id="scan-ov">
  <div class="scan-hdr">
    <button class="scan-close" onclick="closeScanner()">✕</button>
    <div class="scan-title">Scan product barcode</div>
  </div>
  <div class="scan-vw">
    <video id="scan-vid" autoplay playsinline muted></video>
    <div class="scan-frame"></div>
  </div>
  <div class="scan-hint">Point camera at barcode on packaging</div>
  <div class="scan-manual">
    <input type="number" id="manual-bc" placeholder="Or type barcode number…">
    <button onclick="lookupManual()">Find</button>
  </div>
</div>

<script>
// ── DEFAULT STORES — pre-configured, no setup needed ─────────────────────
// Add more branches here as you get their CSV URLs
const DEFAULT_STORES = [
  {
    id: 'store_roundhay',
    name: 'Roundhay Road',
    url: 'https://docs.google.com/spreadsheets/d/e/2PACX-1vQUrvjJHbN8SSivdwz78SEq8wozqOMYbR49GoA5_r2741kXxU4_0Sy-dNj5lWnlGREeaWB4O2hC2nf6/pub?gid=1758983339&single=true&output=csv'
  },
  {
    id: 'store_chapeltown',
    name: 'Chapeltown Road',
    url: 'https://docs.google.com/spreadsheets/d/e/2PACX-1vQShthKPVMTanMGmbyZIv7yOAYtQSRHMGin7zIBeSigI8aZGFuuZyVBPC9WGM3Ail0J0qQaO3wFEfan/pub?gid=1503667492&single=true&output=csv'
  },
  {
    id: 'store_citycenter',
    name: 'City Center',
    url: 'https://docs.google.com/spreadsheets/d/e/2PACX-1vRkfXBRQAGFInNCYJ-_WaqtXOBIHz9kGQ2doOa4V8MIUiHa9SFNpGjwJejSTks5G9-E5zHBgP2ewcjW/pub?gid=985285032&single=true&output=csv'
  },
];

function loadStores(){
  // Always start with DEFAULT_STORES as the base
  // Any stores added via the UI are merged in on top
  const extra = JSON.parse(localStorage.getItem('ls_stores_extra')||'[]');
  return [...DEFAULT_STORES, ...extra];
}
function saveStores(s){
  // Only save stores that aren't already in DEFAULT_STORES
  const defaultIds = DEFAULT_STORES.map(d=>d.id);
  const extra = s.filter(x=>!defaultIds.includes(x.id));
  localStorage.setItem('ls_stores_extra', JSON.stringify(extra));
}

// ── STATE ────────────────────────────────────────────────────────────────
let storeData={};
let currentStoreId=null;
let currentView='brand';
let currentProduct=null;
let waNumber=localStorage.getItem('ls_wa')||'';
let ticks=JSON.parse(localStorage.getItem('ls_ticks')||'{}');
let restocks=JSON.parse(localStorage.getItem('ls_restock')||'{}');
let editingId=null;
let scanStream=null,scanInterval=null;

// ── INIT ─────────────────────────────────────────────────────────────────
document.addEventListener('DOMContentLoaded',()=>{
  document.getElementById('top-date').textContent=new Date().toLocaleDateString('en-GB',{weekday:'long',day:'numeric',month:'long',year:'numeric'});
  if(waNumber) document.getElementById('wa-number').value=waNumber;
  const stores=loadStores();
  currentStoreId=stores[0].id;
  renderStoreTabs();
  fetchStore(currentStoreId);
});

function noStoresHTML(){
  return `<div class="empty-state"><div style="font-size:40px;margin-bottom:12px">🏪</div><strong style="font-size:15px;color:var(--text);display:block;margin-bottom:8px">No stores connected yet</strong><p>Tap <strong>+ Add store</strong> above to connect your first branch.</p></div>`;
}

// ── STORE TABS ───────────────────────────────────────────────────────────
function renderStoreTabs(){
  const stores=loadStores();
  document.getElementById('store-bar').innerHTML=
    stores.map(s=>`<div class="store-tab ${s.id===currentStoreId?'active':''}" onclick="switchStore('${s.id}')" title="Tap to view · Long-press to edit">${s.name}</div>`).join('')+
    `<div class="add-tab" onclick="openAddStore()"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>Add store</div>`;
}

function switchStore(id){
  currentStoreId=id;
  document.getElementById('search-input').value='';
  renderStoreTabs();
  const s=loadStores().find(x=>x.id===id);
  document.getElementById('top-name').textContent=s?s.name:'Low Stock';
  if(!storeData[id]) fetchStore(id);
  else renderAll();
}

// ── MODAL ────────────────────────────────────────────────────────────────
function openAddStore(){
  editingId=null;
  document.getElementById('modal-title').textContent='Add a store';
  document.getElementById('modal-desc').textContent='Connect a branch by pasting its Google Sheet published CSV link.';
  document.getElementById('modal-name').value='';
  document.getElementById('modal-url').value='';
  document.getElementById('modal-del-wrap').style.display='none';
  document.getElementById('modal-save-btn').textContent='Connect store';
  document.getElementById('store-modal').classList.remove('hidden');
}

function openEditStore(id){
  const s=loadStores().find(x=>x.id===id);
  if(!s) return;
  editingId=id;
  document.getElementById('modal-title').textContent='Edit — '+s.name;
  document.getElementById('modal-desc').textContent='Update the connection details for this store.';
  document.getElementById('modal-name').value=s.name;
  document.getElementById('modal-url').value=s.url;
  document.getElementById('modal-del-wrap').style.display='block';
  document.getElementById('modal-save-btn').textContent='Save changes';
  document.getElementById('store-modal').classList.remove('hidden');
}

function openSettings(){
  if(currentStoreId) openEditStore(currentStoreId);
  else openAddStore();
}

function closeModal(){document.getElementById('store-modal').classList.add('hidden')}
function handleModalBg(e){if(e.target===document.getElementById('store-modal'))closeModal()}

function toCSVUrl(url){
  // Already a published CSV/export URL
  if(url.includes('/pub?')||url.includes('tqx=out:csv')||url.includes('/export?')) return url;
  // Extract sheet ID + gid and build export URL
  const m=url.match(/\/spreadsheets\/d\/([a-zA-Z0-9-_]+)/);
  if(!m) return url;
  const gid=(url.match(/gid=(\d+)/)||[])[1]||'0';
  return `https://docs.google.com/spreadsheets/d/${m[1]}/export?format=csv&gid=${gid}`;
}

function saveStore(){
  const name=document.getElementById('modal-name').value.trim();
  const rawUrl=document.getElementById('modal-url').value.trim();
  if(!name){alert('Please enter a store name');return}
  if(!rawUrl){alert('Please paste the Google Sheet URL');return}
  if(!rawUrl.includes('google.com/spreadsheets')){alert('That doesn\'t look like a Google Sheets URL.\n\nMake sure you copy the link from:\nFile → Share → Publish to web → CSV');return}
  const url=toCSVUrl(rawUrl);
  const stores=loadStores();
  if(editingId){
    const i=stores.findIndex(s=>s.id===editingId);
    if(i>-1){stores[i].name=name;stores[i].url=url;}
    delete storeData[editingId];
  } else {
    const id='store_'+Date.now();
    stores.push({id,name,url});
    currentStoreId=id;
  }
  saveStores(stores);
  closeModal();
  renderStoreTabs();
  const s=loadStores().find(x=>x.id===currentStoreId);
  document.getElementById('top-name').textContent=s?s.name:'Low Stock';
  fetchStore(currentStoreId);
}

function deleteStore(){
  const s=loadStores().find(x=>x.id===editingId);
  if(!confirm(`Remove "${s?.name}"?\nThis won't delete anything from Google Sheets.`)) return;
  let stores=loadStores().filter(x=>x.id!==editingId);
  saveStores(stores);
  delete storeData[editingId];
  currentStoreId=stores.length?stores[0].id:null;
  closeModal();
  renderStoreTabs();
  if(currentStoreId) switchStore(currentStoreId);
  else{document.getElementById('main-content').innerHTML=noStoresHTML();document.getElementById('top-name').textContent='Low Stock';}
}

// ── FETCH ────────────────────────────────────────────────────────────────
async function fetchStore(id){
  const store=loadStores().find(s=>s.id===id);
  if(!store) return;
  storeData[id]='loading';
  if(id===currentStoreId) showLoading();
  try{
    const res=await fetch(store.url);
    if(!res.ok) throw new Error(`Server returned ${res.status}. Make sure the sheet is published publicly as CSV.`);
    const text=await res.text();
    if(!text.trim()) throw new Error('Sheet returned empty data. Check the correct tab is published.');
    const {data}=Papa.parse(text,{header:true,skipEmptyLines:true});
    const products=parseProducts(data,id);
    products.forEach(p=>{p.ticked=ticks[p.id]||false;p.restock=restocks[p.id]??null;});
    storeData[id]=products;
    document.getElementById('pill-time').textContent='Updated '+new Date().toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit'});
  }catch(e){storeData[id]={error:e.message};}
  if(id===currentStoreId) renderAll();
}

async function refreshCurrent(){
  const btn=document.getElementById('refresh-btn');
  btn.classList.add('spinning');
  delete storeData[currentStoreId];
  await fetchStore(currentStoreId);
  btn.classList.remove('spinning');
}

// ── PARSE ────────────────────────────────────────────────────────────────
function parseProducts(rows,storeId){
  return rows.filter(r=>{
    const v=fv(r,['inventory','stock','chapeltown','roundhay','qty']);
    return v!==null&&parseInt(v)<0;
  }).map((r,i)=>{
    const stock=parseInt(fv(r,['inventory','stock','chapeltown','roundhay','qty']))||0;
    const sku=String(fv(r,['sku','barcode','upc','ean'])||'').trim();
    const vendor=String(fv(r,['vendor','brand','supplier'])||'').trim();
    const title=String(fv(r,['product title','title','product','name'])||'').trim();
    const variant=String(fv(r,['variant_title','variant','size','colour','color','option'])||'').trim();
    const img=String(fv(r,['image 1','image 2','image','img','photo'])||'').trim();
    const id=`${storeId}||${sku||i}||${variant}`;
    return{id,vendor,title,variant,sku,stock,img:img.startsWith('http')?img:null,category:cat(vendor,title)};
  });
}

function fv(row,keys){
  const rk=Object.keys(row);
  for(const k of keys){
    const m=rk.find(x=>x.toLowerCase().replace(/[^a-z0-9]/g,'').includes(k.replace(/[^a-z0-9]/g,'')));
    if(m&&String(row[m]).trim()!=='') return row[m];
  }
  return null;
}

function cat(v,t){
  const s=(v+' '+t).toLowerCase();
  if(/braid|crochet|twist|xpression|ultra braid|pre.?stretch|kinki|jerry curl|water bulk|beach curl|cork screw/.test(s)) return 'Braids & Crochet';
  if(/wig|lace|weave|extension|yaki|cloud 9|galaxy|aussie|remi|premium|angels|sensationnel/.test(s)) return 'Wigs & Extensions';
  if(/colour|color|dye|tint|schwarzkopf|garnier|crazy|kiss color|adore|cover your gray|wella|tintation/.test(s)) return 'Hair Colour & Dye';
  if(/clipper|tong|wahl|andis|babyliss|geepas|curling|straighten|dial.a.heat/.test(s)) return 'Tools & Electricals';
  if(/comb|brush|roller|rod|magic collection|pintail|weaving thread/.test(s)) return 'Combs & Accessories';
  if(/shampoo|conditioner|lotion|serum|silk infusion|sof n|curl activ|chi |acai|treatment|relaxer/.test(s)) return 'Hair Care';
  if(/makeup|lip|eye|nail|mascara|foundation|eyebrow|pencil|beauty forever|nicka|absolute new york|zazie/.test(s)) return 'Beauty & Makeup';
  if(/soap|deodor|carbolic|brut|musk/.test(s)) return 'Body & Skin';
  return 'Accessories & Other';
}

// ── RENDER ───────────────────────────────────────────────────────────────
function saveWA(){waNumber=document.getElementById('wa-number').value.trim().replace(/\D/g,'');localStorage.setItem('ls_wa',waNumber);alert('✓ Saved');}
function showLoading(){document.getElementById('main-content').innerHTML='<div class="loading"><div class="spinner"></div>Loading live stock data…</div>';}
function renderAll(){updateSummary();renderList();}

function updateSummary(){
  const data=storeData[currentStoreId];
  const products=Array.isArray(data)?data:[];
  const done=products.filter(p=>p.ticked).length;
  document.getElementById('pill-remain').textContent=(products.length-done)+' items to check';
  document.getElementById('pill-done').textContent=done+' checked ✓';
  const allDone=Object.values(storeData).flatMap(d=>Array.isArray(d)?d:[]).filter(p=>p.ticked).length;
  document.getElementById('wa-cnt').textContent=allDone;
  const s=loadStores().find(x=>x.id===currentStoreId);
  document.getElementById('top-name').textContent=s?s.name:'Low Stock';
}

function switchView(v){
  currentView=v;
  document.querySelectorAll('.view-tabs .tab').forEach((t,i)=>t.classList.toggle('active',['brand','category','all'][i]===v));
  renderList();
}

function renderList(){
  const el=document.getElementById('main-content');
  const data=storeData[currentStoreId];
  if(!data||data==='loading'){showLoading();return;}
  if(data.error){
    el.innerHTML=`<div class="error-card"><p>⚠️ Could not load data for this store.</p><code>${data.error}</code></div>
    <div style="padding:14px 12px"><p style="font-size:13px;color:var(--muted);line-height:1.6;margin-bottom:10px">Make sure you have published the sheet:<br><strong>File → Share → Publish to web → select tab → CSV → Publish</strong></p>
    <button onclick="openSettings()" style="background:var(--text);color:#fff;border:none;border-radius:var(--rs);padding:11px 16px;font-size:13px;font-weight:600;cursor:pointer;font-family:var(--font)">Update sheet URL</button></div>`;
    return;
  }
  const q=document.getElementById('search-input').value.toLowerCase();
  let ps=data.filter(p=>!q||p.vendor.toLowerCase().includes(q)||p.title.toLowerCase().includes(q)||(p.sku&&p.sku.includes(q))||(p.variant&&p.variant.toLowerCase().includes(q)));
  if(!ps.length&&!q){el.innerHTML='<div class="empty-state">🎉<br><br><strong style="color:var(--text)">Nothing low on stock!</strong><br>Everything is above threshold.</div>';return;}
  if(!ps.length){el.innerHTML='<div class="empty-state">No products match your search.</div>';return;}
  if(currentView==='all'){el.innerHTML=ps.map(cardHTML).join('');return;}
  const key=currentView==='brand'?'vendor':'category';
  const groups={};
  ps.forEach(p=>{if(!groups[p[key]])groups[p[key]]=[];groups[p[key]].push(p);});
  const sorted=Object.entries(groups).sort((a,b)=>Math.min(...a[1].map(p=>p.stock))-Math.min(...b[1].map(p=>p.stock)));
  let html='';
  if(currentView==='brand'){
    const byCat={};
    sorted.forEach(([k,ps])=>{const c=ps[0].category;if(!byCat[c])byCat[c]=[];byCat[c].push([k,ps]);});
    Object.entries(byCat).sort().forEach(([c,brands])=>{
      html+=`<div class="sec-head">${c}<span style="font-size:10px;font-weight:400">${brands.reduce((s,[,p])=>s+p.length,0)} items</span></div>`;
      brands.forEach(([b,ps])=>html+=groupHTML(b,ps,false));
    });
  } else sorted.forEach(([c,ps])=>html+=groupHTML(c,ps,true));
  el.innerHTML=html;
}

function groupHTML(name,ps,isCat){
  const min=Math.min(...ps.map(p=>p.stock));
  const total=ps.reduce((s,p)=>s+p.stock,0);
  const done=ps.filter(p=>p.ticked).length;
  const dot=min<=-5?'#b91c1c':min<=-2?'#d97706':'#9ca3af';
  const bc=min<=-4?'b-red':'b-amber';
  const sub=done>0?`${done}/${ps.length} <span style="color:var(--green);font-weight:600">done ✓</span>`:`${ps.length} item${ps.length>1?'s':''}`;
  const esc=name.replace(/\\/g,'\\\\').replace(/'/g,"\\'");
  return `<div class="group-row" onclick="showGroup('${esc}',${isCat})"><div class="g-dot" style="background:${dot}"></div><div class="g-info"><div class="g-name">${name}</div><div class="g-sub">${sub}</div></div><div class="g-right"><span class="g-badge ${bc}">${total}</span><span class="chevron">›</span></div></div>`;
}

function showGroup(name,isCat){
  const key=isCat?'category':'vendor';
  const items=(storeData[currentStoreId]||[]).filter(p=>p[key]===name);
  document.getElementById('main-content').innerHTML=
    `<div class="back-bar" onclick="renderList()"><span style="font-size:18px;color:var(--muted)">‹</span><span style="font-size:13px;font-weight:500;margin-left:4px">${name}</span></div>`+items.map(cardHTML).join('');
}

function cardHTML(p){
  const qty=p.restock!=null?`<span class="p-qty qty-pos">+${p.restock}</span>`:`<span class="p-qty qty-neg">${p.stock}</span>`;
  const img=p.img?`<img src="${p.img}" alt="" loading="lazy" onerror="this.style.display='none'">`:`<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#ccc" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>`;
  const eid=p.id.replace(/[^a-zA-Z0-9]/g,'_');
  return `<div class="product-card ${p.ticked?'ticked':''}" id="c_${eid}" onclick="openPanel('${p.id.replace(/'/g,"\\'")}')">
    <div class="done-stripe"></div>
    <div class="p-img-wrap">${img}</div>
    <div class="p-body"><div class="p-brand">${p.vendor}</div><div class="p-name">${p.title}</div>${p.variant?`<div class="p-variant">${p.variant}</div>`:''}</div>
    <div class="p-right">${qty}<div class="tick-circle ${p.ticked?'done':''}" onclick="quickTick(event,'${p.id.replace(/'/g,"\\'")}')"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg></div></div>
  </div>`;
}

// ── TICK ─────────────────────────────────────────────────────────────────
function quickTick(e,id){
  e.stopPropagation();
  const data=storeData[currentStoreId];
  if(!Array.isArray(data)) return;
  const p=data.find(x=>x.id===id);
  if(!p) return;
  p.ticked=!p.ticked;
  ticks[id]=p.ticked;
  localStorage.setItem('ls_ticks',JSON.stringify(ticks));
  updateSummary();
  const eid=id.replace(/[^a-zA-Z0-9]/g,'_');
  const card=document.getElementById('c_'+eid);
  if(card){
    card.className=`product-card ${p.ticked?'ticked':''}`;
    const tc=card.querySelector('.tick-circle');
    if(tc) tc.className=`tick-circle ${p.ticked?'done':''}`;
  }
}

// ── PANEL ────────────────────────────────────────────────────────────────
function openPanel(id){
  const data=storeData[currentStoreId];
  if(!Array.isArray(data)) return;
  const p=data.find(x=>x.id===id);
  if(!p) return;
  currentProduct=p;
  document.getElementById('pd-brand').textContent=p.vendor;
  document.getElementById('pd-title').textContent=p.title;
  document.getElementById('pd-variant').textContent=p.variant||'';
  document.getElementById('pd-cat').textContent=p.category;
  document.getElementById('pd-stock').textContent='Stock: '+p.stock;
  document.getElementById('pd-sku').textContent=p.sku?'SKU: '+p.sku:'No barcode on file';
  const hero=document.getElementById('pd-hero');
  hero.innerHTML=p.img?`<img src="${p.img}" alt="" style="max-height:170px;max-width:100%;object-fit:contain" onerror="this.style.display='none'">`:`<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#ccc" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>`;
  const bcw=document.getElementById('bc-wrap');
  const sk=p.sku.replace(/\D/g,'');
  if(sk.length>=8){bcw.style.display='block';try{JsBarcode('#bc-svg',sk,{format:'EAN13',width:2,height:55,displayValue:true,fontSize:11,margin:7,lineColor:'#18160f',background:'#ffffff'});}catch(e){try{JsBarcode('#bc-svg',p.sku,{format:'CODE128',width:2,height:50,displayValue:true,fontSize:11,margin:6});}catch(e2){bcw.style.display='none';}}}else bcw.style.display='none';
  document.getElementById('step-val').textContent=p.restock??0;
  const db=document.getElementById('done-btn');
  db.className=`done-btn ${p.ticked?'active':''}`;
  db.innerHTML=`<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg> ${p.ticked?'Marked done':'Mark done'}`;
  document.getElementById('saved-msg').style.display='none';
  document.getElementById('panel-ov').classList.add('open');
}

function handlePanelBg(e){if(e.target===document.getElementById('panel-ov'))closePanel();}
function closePanel(){document.getElementById('panel-ov').classList.remove('open');currentProduct=null;}
function step(d){const el=document.getElementById('step-val');el.textContent=Math.max(0,(parseInt(el.textContent)||0)+d);}

function saveCount(){
  if(!currentProduct) return;
  const val=parseInt(document.getElementById('step-val').textContent)||0;
  currentProduct.restock=val;currentProduct.ticked=true;
  ticks[currentProduct.id]=true;restocks[currentProduct.id]=val;
  localStorage.setItem('ls_ticks',JSON.stringify(ticks));localStorage.setItem('ls_restock',JSON.stringify(restocks));
  document.getElementById('done-btn').className='done-btn active';
  document.getElementById('done-btn').innerHTML=`<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg> Marked done`;
  document.getElementById('saved-msg').style.display='block';
  updateSummary();setTimeout(()=>{closePanel();renderList();},900);
}

function toggleTick(){
  if(!currentProduct) return;
  currentProduct.ticked=!currentProduct.ticked;
  ticks[currentProduct.id]=currentProduct.ticked;
  localStorage.setItem('ls_ticks',JSON.stringify(ticks));
  const db=document.getElementById('done-btn');
  db.className=`done-btn ${currentProduct.ticked?'active':''}`;
  db.innerHTML=`<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg> ${currentProduct.ticked?'Marked done':'Mark done'}`;
  updateSummary();
}

// ── WHATSAPP ─────────────────────────────────────────────────────────────
function sendWhatsApp(){
  const num=waNumber||document.getElementById('wa-number').value.trim().replace(/\D/g,'');
  if(!num){alert('Please save the manager WhatsApp number first');return;}
  const date=new Date().toLocaleDateString('en-GB',{weekday:'long',day:'numeric',month:'short',year:'numeric'});
  let msg=`*Low Stock Update — ${date}*\n\n`;
  let any=false;
  loadStores().forEach(store=>{
    const data=storeData[store.id];
    if(!Array.isArray(data)) return;
    const done=data.filter(p=>p.ticked);
    const pending=data.filter(p=>!p.ticked);
    if(!done.length&&!pending.length) return;
    msg+=`*${store.name}*\n`;
    if(done.length){any=true;msg+=`✅ ${done.length} item${done.length>1?'s':''} checked:\n`;done.forEach(p=>{const r=p.restock!=null?` (+${p.restock} restocked)`:'';msg+=`  • ${p.vendor} — ${p.title}${p.variant?' ('+p.variant+')':''}${r}\n`;});}
    if(pending.length) msg+=`⏳ ${pending.length} still to check\n`;
    msg+='\n';
  });
  if(!any) msg+='No items checked yet today.\n';
  msg+=`_Sent from Low Stock App_`;
  window.open(`https://wa.me/${num}?text=${encodeURIComponent(msg)}`,'_blank');
}

// ── SCANNER ──────────────────────────────────────────────────────────────
function openScanner(){
  document.getElementById('scan-ov').classList.add('open');
  navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}})
    .then(s=>{scanStream=s;document.getElementById('scan-vid').srcObject=s;
      if('BarcodeDetector' in window){const d=new BarcodeDetector({formats:['ean_13','ean_8','code_128','upc_a']});
        scanInterval=setInterval(async()=>{try{const c=await d.detect(document.getElementById('scan-vid'));if(c.length){closeScanner();lookupBarcode(c[0].rawValue);}}catch(e){}},400);}
    }).catch(()=>alert('Camera access denied. Use manual entry below.'));
}
function closeScanner(){
  document.getElementById('scan-ov').classList.remove('open');
  if(scanInterval){clearInterval(scanInterval);scanInterval=null;}
  if(scanStream){scanStream.getTracks().forEach(t=>t.stop());scanStream=null;}
}
function lookupManual(){lookupBarcode(document.getElementById('manual-bc').value.trim());closeScanner();}
function lookupBarcode(code){
  const data=storeData[currentStoreId];
  if(!Array.isArray(data)) return;
  const c=code.replace(/\D/g,'');
  const p=data.find(x=>x.sku&&x.sku.replace(/\D/g,'')===c);
  if(p) openPanel(p.id);
  else alert('Product not found: '+code);
}
</script>
</body>
</html>
